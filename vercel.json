{
  "buildCommand": "cd ../.. && bun install && cd packages/app && bun run build",
  "outputDirectory": "packages/app/dist",
  "installCommand": "echo 'Dependencies installed in buildCommand'",
  "framework": "vite"
}
